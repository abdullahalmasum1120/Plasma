import 'package:flutter/material.dart';

class MySizes{
  static const double defaultSpace = 16;
  static const double defaultRadius = 8;
  static const double largeIconSize = 96;
  // static const Size minButtonSize = Size(0, 0);
  static const Size maxButtonSize = Size(double.infinity, 0);
}